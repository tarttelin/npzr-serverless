import { expect } from 'chai';

describe("Cannot double click when releasing a card to select the card behind", () => {
   it("Should prevent cards from being moved until 500ms after the previously selected card is released", () => {
       fail("Not implemented")
   })
});